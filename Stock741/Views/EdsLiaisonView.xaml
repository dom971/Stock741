<UserControl x:Class="Stock741.Views.EdsLiaisonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
            <Label Content="Cible:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Width="120"
                     Text="{Binding CibleSelectionne, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,10,0"/>
            <Label Content="EDS:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <ComboBox Width="300"
                      ItemsSource="{Binding EdsList}"
                      SelectedItem="{Binding EdsSelectionne}"
                      DisplayMemberPath="Nom"
                      Margin="0,0,10,0"/>
            <Button Content="Ajouter"    Command="{Binding AjouterCommand}"    Margin="5,0,0,0"/>
            <Button Content="Modifier"   Command="{Binding ModifierCommand}"   Margin="5,0,0,0"/>
            <Button Content="Supprimer"  Command="{Binding SupprimerCommand}"  Margin="5,0,0,0"/>
            <Button Content="Actualiser" Command="{Binding ActualiserCommand}" Margin="15,0,0,0"/>
        </StackPanel>

        <TextBlock Grid.Row="1"
                   Text="{Binding ErreurGlobale}"
                   Foreground="Red"
                   FontWeight="SemiBold"
                   Margin="0,0,0,5"
                   Visibility="{Binding ErreurGlobale, Converter={StaticResource StringToVisibilityConverter}}"/>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding EdsLiaisons}"
                  AutoGenerateColumns="False"
                  SelectedItem="{Binding EdsLiaisonSelectionnee}"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  EnableRowVirtualization="True"
                  EnableColumnVirtualization="True"
                  VirtualizingPanel.VirtualizationMode="Recycling">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id"    Binding="{Binding Id}"       Width="Auto"/>
                <DataGridTextColumn Header="Cible" Binding="{Binding Cible}"    Width="Auto"/>               
                <DataGridTextColumn Header="Code"  Binding="{Binding Eds.Cnx}"  Width="*"/>
                <!--<DataGridTextColumn Header="EDS"   Binding="{Binding Eds.Nom}"  Width="*"/>-->
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>